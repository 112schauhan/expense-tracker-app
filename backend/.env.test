# Test Environment Configuration
NODE_ENV=test
JWT_SECRET=test-jwt-secret-for-testing-only
JWT_EXPIRES_IN=1h
LOG_LEVEL=error

# Mock database URL (not needed for unit tests)
DATABASE_URL=postgresql://test:test@localhost:5432/test_db